# Optional gateway origin for browser runtime (recommended for internet testers).
# If unset:
# - localhost browser -> defaults to localhost service ports
# - non-localhost browser -> defaults to http(s)://<current-host>:4000
VITE_GATEWAY_URL=http://localhost:4000

# Optional explicit overrides.
# VITE_API_URL=http://localhost:4001
# VITE_INTEGRATION_API_URL=http://localhost:4003
# VITE_WS_URL=http://localhost:4001
